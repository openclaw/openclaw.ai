---
import { FileText, Megaphone, Menu, X } from '@lucide/astro';
import { siDiscord, siGithub } from 'simple-icons';

type SimpleIcon = {
  kind: 'simple';
  path: string;
};

type ComponentIcon = {
  kind: 'component';
  component: typeof FileText;
};

type NavIcon = SimpleIcon | ComponentIcon;

interface NavItem {
  href: string;
  label: string;
  external?: boolean;
  icon?: NavIcon;
}

interface Props {
  active?: 'home' | 'blog' | 'docs' | 'ecosystem' | 'integrations';
}

const { active = 'home' } = Astro.props;
const items: NavItem[] = [
  { href: '/', label: 'Product' },
  { href: '/ecosystem', label: 'Ecosystem' },
  { href: '/integrations', label: 'Integrations' },
  { href: '/blog', label: 'Blog', icon: { kind: 'component', component: Megaphone } },
  {
    href: 'https://docs.openclaw.ai/',
    label: 'Docs',
    external: true,
    icon: { kind: 'component', component: FileText },
  },
  {
    href: 'https://discord.com/invite/clawd',
    label: 'Discord',
    external: true,
    icon: { kind: 'simple', path: siDiscord.path },
  },
  {
    href: 'https://github.com/openclaw/openclaw',
    label: 'GitHub',
    external: true,
    icon: { kind: 'simple', path: siGithub.path },
  },
];

function isActive(item: NavItem) {
  if (active === 'home') return item.href === '/';
  if (active === 'blog') return item.href === '/blog';
  if (active === 'docs') return item.label === 'Docs';
  if (active === 'ecosystem') return item.href === '/ecosystem';
  if (active === 'integrations') return item.href === '/integrations';
  return false;
}
---

<header class="site-topbar">
  <a href="/" class="site-brand" aria-label="OpenClaw home">
    <img src="/favicon.svg" alt="" />
    <span>OpenClaw</span>
  </a>
  <nav class="site-nav site-nav-desktop" aria-label="Main">
    {items.map((item) => (
      <a
        href={item.href}
        class:list={['site-nav-link', { active: isActive(item) }]}
        target={item.external ? '_blank' : undefined}
        rel={item.external ? 'noopener' : undefined}
        aria-current={isActive(item) && !item.external ? 'page' : undefined}
      >
        {item.icon?.kind === 'simple' && (
          <svg class="site-nav-icon site-nav-icon-simple" viewBox="0 0 24 24" aria-hidden="true">
            <path d={item.icon.path} />
          </svg>
        )}
        {item.icon?.kind === 'component' && (
          <item.icon.component class="site-nav-icon site-nav-icon-lucide" size={14} aria-hidden="true" />
        )}
        {item.label}
      </a>
    ))}
  </nav>
  <details class="site-menu">
    <summary class="site-menu-toggle" aria-label="Toggle main navigation">
      <span class="site-menu-icon site-menu-icon-open" aria-hidden="true"><Menu size={18} /></span>
      <span class="site-menu-icon site-menu-icon-close" aria-hidden="true"><X size={18} /></span>
    </summary>
    <nav class="site-menu-panel" aria-label="Main">
      {items.map((item) => (
        <a
          href={item.href}
          class:list={['site-nav-link', { active: isActive(item) }]}
          target={item.external ? '_blank' : undefined}
          rel={item.external ? 'noopener' : undefined}
          aria-current={isActive(item) && !item.external ? 'page' : undefined}
        >
          {item.icon?.kind === 'simple' && (
            <svg class="site-nav-icon site-nav-icon-simple" viewBox="0 0 24 24" aria-hidden="true">
              <path d={item.icon.path} />
            </svg>
          )}
          {item.icon?.kind === 'component' && (
            <item.icon.component class="site-nav-icon site-nav-icon-lucide" size={14} aria-hidden="true" />
          )}
          {item.label}
        </a>
      ))}
    </nav>
  </details>
</header>

<style>
  .site-topbar {
    position: sticky;
    top: 0;
    z-index: 90;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    min-height: 62px;
    padding: 0 82px 0 32px;
    border-bottom: 1px solid var(--border-subtle);
    background: color-mix(in srgb, var(--bg-deep) 88%, transparent);
    backdrop-filter: blur(18px);
  }

  .site-brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--text-primary);
    text-decoration: none;
    font-family: var(--font-display);
    font-weight: 700;
    letter-spacing: 0;
  }

  .site-brand img {
    width: 24px;
    height: 24px;
    filter: drop-shadow(0 0 12px var(--logo-glow));
  }

  .site-nav {
    display: flex;
    align-items: center;
    gap: 8px;
    overflow-x: auto;
    white-space: nowrap;
  }

  .site-menu {
    display: none;
  }

  .site-nav-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    min-height: 34px;
    padding: 0 12px;
    border: 1px solid transparent;
    border-radius: 8px;
    color: var(--text-muted);
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.16s ease, background 0.16s ease, border-color 0.16s ease;
  }

  .site-nav-icon {
    width: 14px;
    height: 14px;
    flex: 0 0 auto;
    stroke-width: 2.1;
  }

  .site-nav-icon-simple {
    fill: currentColor;
    stroke: none;
  }

  .site-nav-icon-lucide {
    fill: none;
  }

  .site-nav-link:hover,
  .site-nav-link.active {
    color: var(--text-primary);
    background: var(--surface-card);
    border-color: var(--border-subtle);
  }

  .site-nav-link.active {
    color: var(--coral-bright);
  }

  @media (max-width: 720px) {
    .site-topbar {
      align-items: center;
      flex-direction: row;
      gap: 16px;
      min-height: 62px;
      padding: 0 58px 0 16px;
    }

    .site-nav.site-nav-desktop {
      display: none;
    }

    .site-menu {
      position: relative;
      display: block;
      margin-left: auto;
    }

    .site-menu-toggle {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      border: 1px solid var(--border-subtle);
      border-radius: 8px;
      background: var(--surface-card);
      color: var(--text-secondary);
      cursor: pointer;
      list-style: none;
      transition: border-color 0.16s ease, background 0.16s ease, color 0.16s ease;
    }

    .site-menu-toggle::-webkit-details-marker {
      display: none;
    }

    .site-menu-toggle:hover,
    .site-menu[open] .site-menu-toggle {
      border-color: var(--border-accent);
      background: var(--surface-card-strong);
      color: var(--text-primary);
    }

    .site-menu-toggle:focus-visible {
      outline: 2px solid var(--cyan-bright);
      outline-offset: 2px;
    }

    .site-menu-icon {
      display: inline-flex;
      line-height: 0;
    }

    .site-menu-icon-close,
    .site-menu[open] .site-menu-icon-open {
      display: none;
    }

    .site-menu[open] .site-menu-icon-close {
      display: inline-flex;
    }

    .site-menu-panel {
      position: fixed;
      top: 62px;
      right: 12px;
      left: 12px;
      z-index: 92;
      display: grid;
      gap: 4px;
      padding: 8px;
      border: 1px solid var(--border-subtle);
      border-radius: 8px;
      background: color-mix(in srgb, var(--bg-deep) 96%, transparent);
      box-shadow: 0 16px 36px var(--surface-overlay);
      backdrop-filter: blur(18px);
    }

    .site-menu-panel .site-nav-link {
      width: 100%;
      min-height: 42px;
      justify-content: flex-start;
      padding: 0 12px;
      font-size: 0.9rem;
    }
  }
</style>
