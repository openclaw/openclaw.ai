---
import IntegrationsPage from "../../components/templates/IntegrationsPage.astro";
import { locales } from "../../paraglide/runtime";

export function getStaticPaths() {
    return locales
        .filter((lang) => lang !== "en")
        .map((lang) => ({ params: { lang } }));
}
---

<IntegrationsPage />
