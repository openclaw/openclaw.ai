---
import ShowcasePage from "../../components/templates/ShowcasePage.astro";
import { locales } from "../../paraglide/runtime";

export function getStaticPaths() {
    return locales
        .filter((lang) => lang !== "en")
        .map((lang) => ({ params: { lang } }));
}
---

<ShowcasePage />
