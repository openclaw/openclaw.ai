---
import ShoutoutsPage from "../../components/templates/ShoutoutsPage.astro";
import { locales } from "../../paraglide/runtime";

export function getStaticPaths() {
    return locales
        .filter((lang) => lang !== "en")
        .map((lang) => ({ params: { lang } }));
}
---

<ShoutoutsPage />
